{
  "name": "flowledger",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "api",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,app -c green,blue \"npm run dev -w @flowledger/api\" \"npm run dev -w @flowledger/app\"",
    "dev:api": "npm run dev -w @flowledger/api",
    "dev:app": "npm run dev -w @flowledger/app",
    "build": "npm run build -w @flowledger/api && npm run build -w @flowledger/app",
    "start": "npm run start -w @flowledger/api",
    "lint": "npm run lint -w @flowledger/api && npm run lint -w @flowledger/app",
    "clean": "rm -rf dist packages/*/dist packages/*/node_modules node_modules"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
